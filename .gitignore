# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
**/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.dev.vars
**/.dev.vars

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code config (user-specific, not project source).
# Ignore everything in .claude/ by default, then selectively re-include
# project-portable content (skills + agents intended to travel with the repo).
/.claude/*
!/.claude/skills/
/.claude/skills/*
!/.claude/skills/pricing-advisor/
# Marketing operating layer (curated from Corey Haines' marketingskills, MIT) — 20 Jul 2026
!/.claude/skills/product-marketing/
!/.claude/skills/marketing-plan/
!/.claude/skills/customer-research/
!/.claude/skills/marketing-psychology/
!/.claude/skills/content-strategy/
!/.claude/skills/seo-audit/
!/.claude/skills/programmatic-seo/
!/.claude/skills/ai-seo/
!/.claude/skills/community-marketing/
!/.claude/skills/public-relations/
!/.claude/skills/copywriting/
!/.claude/skills/cro/
!/.claude/skills/signup/
!/.claude/skills/onboarding/
!/.claude/skills/emails/
!/.claude/skills/churn-prevention/
!/.claude/skills/referrals/
!/.claude/skills/pricing/
# Project design/build systems — these govern app SOURCE output, so they must
# travel with the repo (23 Jul 2026). diagram-design was PC-only until now,
# which blocked the benchmark #9b/#9c diagram component builds on the laptop.
!/.claude/skills/diagram-design/
!/.claude/skills/game-design/
!/.claude/agents/
/.claude/agents/*
!/.claude/agents/pricing-advisor.md

# Cloudflare
/.wrangler

# Visual QA screenshots (large, regenerable)
/visual-qa-screenshots
/visual-qa-audit

# API keys and secrets
/Master Brief Document and Working Instructions/Claude API Key

# Research library — track text (.md) only; scan/binary subfolders stay ignored below
/research/**
!/research/**/
!/research/**/*.md

# Scanned reference materials (large binary files)
# NOTE: /research/past-papers is deliberately NOT ignored here. Parsed paper
# TEXT (.md) is tracked via the /research/** + !*.md rules above so it syncs
# across machines for the Oracle; only its scanned image binaries stay ignored.
# Ignore the scans, but keep any ANALYSIS text (.md) — it is project knowledge
# and was silently PC-only until 23 Jul 2026. A bare dir-ignore cannot be
# re-included per-file, hence the /* + !*.md shape.
/Examples for English and VR/*
!/Examples for English and VR/*.md
/Micro-Lesson Research/*
!/Micro-Lesson Research/*.md
/research/kingsbury
/research/maths-reference
/Lesson Plan Examples
/difficulty-scores

# Progress tracker screenshots (large binary files)
/Progress Tracker Content

# Temp/working files
_tmp_*
_diagram-audit/
dev-review-notes.json

# D1 backups + staging copies — contain sensitive user data, never commit
/backups/*.sql
/backups/*.sql.gpg
/backups/*.sql.gz
/backups/*.db

# Wrangler local state (nested)
workers/**/.wrangler

# Authoring-wave working files. The data-generation scripts default here now
# (they used to default to one machine's Claude scratchpad, so their defaults
# only worked on the laptop). Contents are intermediate Oracle output —
# regenerable, and the real runs pass --data explicitly.
/scripts/data-generation/work/

# Batch processing scripts (one-off, not app code)
scripts/bias-batch-*
scripts/oracle-batch-*
scripts/comprehension-length-bias.json
scripts/write-remaining-batches.js

# Recovery toolkit generated artefacts (regenerable from scripts/recovery/*.py)
/recovery-output

# Process-gate artefacts (Plan A v2.1 — produced by scripts/process-gates/*.mjs)
# Contain row counts, schema fingerprints, Worker SHA. Regenerable by re-running
# the relevant script. Hooks read these locally; not needed in version control.
/process-gate-artefacts

# Audit outputs — generated by audit scripts, not source
/brand-assets/mobile-audit
/scripts/validation/lint-report.json

# Dev-only rendered email previews (scripts/preview-emails.mjs) — regenerable, never committed
/demo-preview
scripts/.metrics.env
